import { platformDesign } from '../constants/platformDesign';

/** Hook-style accessor so screens stay consistent with token-driven UI. */
export function usePlatformDesign() {
  return platformDesign;
}
