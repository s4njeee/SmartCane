/**
 * Platform entry for BottomTabBar.
 * Metro resolves .ios / .android; this fallback keeps web/types on the iOS look.
 */
export { default } from './BottomTabBar.ios';
export type { TabKey } from './tabTypes';
